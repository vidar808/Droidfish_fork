[pytest]
asyncio_mode = auto
timeout = 120
testpaths = integration e2e stress error android
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    tls: marks tests requiring TLS
    relay: marks tests requiring relay server
    engine: marks tests that validate real engines
    android: marks tests requiring Android emulator
